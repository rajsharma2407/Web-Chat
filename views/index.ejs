<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Video Chat app</title>
    <script src="https://kit.fontawesome.com/745bd8c243.js" crossorigin="anonymous"></script>
    <script>const myId = "<%= id %>"</script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>

    <style>
        *{
            margin:0;
            padding:0;
            box-sizing: border-box;
        }
        body{
            background:#fff;
        }
        .container{
            margin:20px auto;
            padding-top: 15px;
            width:60%;
            background:rgba(230,230,230,0.1);
            position: relative;
            height:92vh;
            font-family: sans-serif;
            box-shadow: 0 0 10px gray;
            border-radius: 20px 20px 0 0;
        }
        #text{
            width:95%;
            height:40px;
            border:none;
            float:left;
            border:1px solid lightblue;
        }
        .box{
            width:100%;
            position: absolute;
            bottom: -40px;
            box-shadow: 0 5px 15px gray;
        }
        .btn{
            width:5%;
            height:40px;
            border:none;
            float:right;
            background:rgb(85, 66, 192);
            color:white;
        }
        .msg-box{
            width:100%;
            height:100%;
            overflow:hidden auto;
            position:relative;
        }
        .nav{
            position:absolute;
            left:25px;
            top:30px;
            display: flex;
            height:200px;
            font-size: 30px;
            display:flex;
            flex-direction: column;
            justify-content: space-around;
            align-items: center;
            width:100px;
        }
        .nav i{
            cursor:pointer;
        }
        .nav i:hover{
            color:gray;
            transition:0.2s ease all;
        }
        .add{
            border-radius: 50%;
            font-size: 50px;
            background:#4320aa;
            color:white;
            position:absolute;
            right:20px;
            top:20px;
            text-align: center;
            padding:0 7px;
            display: none;
        }
		@media (max-width: 800px){
            .container{
                width:100%;
                margin:0;
                border-radius: 0;
            }
            .btn{
                width:10%;
            }
            #text{
                width:90%;
            }
            .nav{
                display: none;
                height:30px;
                flex-direction: row;
                width:50%;
                transform:translateX(-100%);

            }
            .nav i{
                background:white;
                padding:5px;
                border-radius: 30px;

            }
            .add{
                display: block;
            }
        }
        .visible{
            transition: 10s cubic-bezier(0.6, 0.04, 0.98, 1.335) all;
            display: flex;  
            transform:translateX(10%)
        }
        nav{
            transition: 10s cubic-bezier(0.6, 0.04, 0.98, 1.335) all;
        }
    </style>
</head>
<body>
        <div class="container">
            <div class="msg-box"></div>
            <div class="box">
                <input name="text" id="text">
                <input type="button" value="Send" class="btn">
            </div>
        </div>
        <span class="add" id="add">+</span>
         <nav class="nav">
           <a href="/video/<%= id %>"><i class="fas fa-video"></i></a>
           <a href="/audio/<%= id %>"><i class="fas fa-microphone"></i></a>
           <a href="/chat/<%= id %>"><i class="far fa-envelope"></i></a>
            </nav>
    <script src="/script.js"></script>
</body>
</html>